void main() {
  // bool isTrue = false;

  // if
  /*
  if (isTrue) {
    print("True");
  } else if (!isTrue) {
    print("False");
  }
  */

  // Switch
  /**
   * switch (비교 대상){
   *  case (조건식 1):
   *  case (조건식 1):
   *  case (조건식 1):
   *  default :
   * }
   * 
   */

  int num = 101;

  switch (num) {
    case 1:
      print(num);
    case 2:
      print(num);
    case 3:
      print(num);
    case 4:
      print(num);
    case 5:
      print(num);
    case > 100:
      print("100이상!");
    default:
      print("감자감자");
  }
}
